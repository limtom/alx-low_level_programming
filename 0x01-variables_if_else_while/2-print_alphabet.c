#include <stdio.h>

/**
 * main - main entry
 * Description:- Prints alphabets
 * Return (0) Success
 */

int main(void)
{
char alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)
{
putchar(alpha);
}
return (0);
}
