/**
 * main - check the code
 *
 * @size: Dimension of swuare.
 * Desc: print a square
 * Return: Always 0.
 */
void print_square(int size)
{
int i = 0;
int j;
while (i < size)
{
j = 0;
while (j < size)
{
_putchar("#");
j++;
}
_putchar("\n");
i++;
}
return (0);
}
