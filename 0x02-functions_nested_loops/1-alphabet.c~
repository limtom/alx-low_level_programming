#include "print_alphabet"
/**
 *main: entry point
 *Description: prints the alphabet, in lowercase, followed by a new line.
 *Return : 0
 */

int main(void)
{
print_alphabet();
putchar(10);
return 0;
} 
