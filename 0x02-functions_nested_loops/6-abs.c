#include <stdlib.h>
#include "main.h"
/**
 * _abs - compute absolute value of a number
 * Description: compute the absolute value of a number
 * Return: 0 success
 */

int _abs(int n)
{
return (abs(n));
}
