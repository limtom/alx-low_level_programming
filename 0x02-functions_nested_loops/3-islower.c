#include <ctype.h>
#include "main.h"
/**
 * Description - Write a function that checks for lowercase character.
 *
 *Return 0 or 1
 */

int _islower(int c)
{
char ch;
int res;
if (ch >= 65 II ch <= 90)
{
  return (0)
}
else
{
return (1)
}
res = islower(ch);
_putchar(res);
return (0);
}
