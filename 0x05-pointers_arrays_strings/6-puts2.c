#include "main.h"
#include <string.h>
/**
* put2- print every other character of a string starting with the first on a newline
*@s: char parameter
* Return: 0
*/

void put2(char *s)
{
int len, i;
len = strlen(s);
for(i = 0; i < len-1; i++)
{
str[i];
}
}