#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strcpy- copy the string pointed to by the scr
* @dest: string to copy to.
* @src: string to copy.
* Return: dest
*/
char *_strcpy(char *dest, char *src)
{
strcpy(dest,src);
return (dest);
}
